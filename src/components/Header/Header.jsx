import styles from './header.module.css';

const Header = () => {
    return <>
        <header className={styles.header}>
            <div className={styles.header__container}>LOGO</div>
        </header>
    </>
}

export default Header;